/**
 *  - Puppeter: Provides a high-level API to control Chrome over the DevTools Protocol. 
 *  - Async-file: Adapts the Node.js File System API (fs) for use with TypeScript async/await.
 *  - Randomstring: Library to help you create random strings.
 *  - Request: Designed to be the simplest way possible to make http calls.
 */
const puppeteer = require("puppeteer");
const fs = require('async-file');
const randomstring = require("randomstring");
const request = require("request");
// const { readFile } = require('./path/to/.exports');
